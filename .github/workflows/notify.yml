# notify.yml is replaced by auto-merge.yml which now handles both
# PR merging (via PAT) and ZyntaWRT dispatch in one workflow_run job.
# This file is kept as a no-op to avoid breaking existing references.
name: Notify Firmware Build (deprecated)

on:
  workflow_dispatch:  # manual only, never fires automatically

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is superseded by auto-merge.yml"
